<configuration>

  <property>
    <name>dfs.data.dir</name>
    <value>{{ hdfs_dfs_data_dir | join(',') }}</value>
  </property>

  <property>
    <name>dfs.datanode.synconclose</name>
    <value>true</value>
  </property>

  <property>
    <name>dfs.durable.sync</name>
    <value>true</value>
  </property>

  <property>
    <name>dfs.name.dir</name>
    <value>{{ hdfs_dfs_name_dir | join(',') }}</value>
  </property>

  <property>
    <name>fs.checkpoint.dir</name>
    <value>{{ hdfs_fs_checkpoint_dir | join(',') }}</value>
  </property>

</configuration>
